class NeoCyclist {
  NeoCyclist({
    dynamic onTrigger,
    dynamic onToggle,
    dynamic getTime,
  }) {
    throw UnsupportedError(
      'NeoCyclist is not supported in the Dart/Flutter port.',
    );
  }
}
